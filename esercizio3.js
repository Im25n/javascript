function CalcoloArea()  
     let forma = prompt ("inserisci la forma di cui vuoi calcolare l'area:rettangolo, triangolo,cerchio")
console.log(forma)

// imposta le regole per l forme geometriche 


if ( forma === "rettangolo"){
    // logica calcolo area rettangolo
    console.log ("calcolo forma rettangolo IN")
 const base = prompt ("inserisci il valore della base")
const altezza = prompt ("inserisci dell' altezza ") 
console.log ("calcolo forma rettangolo valori bh" + base + altezza)
} 
if (forma === "triangolo") {
//logica calcolo area triangolo
console.log("calcolo forma traingolo IN")

}
if (forma === "cerchio")  {
    //logica calcolo area cerchio
    console.log ("calcolo forma cerchio IN")
}
